---
phase: 03-core-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/icons/LinkedIn.tsx
  - components/icons/GitHub.tsx
  - components/icons/Email.tsx
  - components/icons/index.ts
  - components/icons/tech/TypeScript.tsx
  - components/icons/tech/React.tsx
  - components/icons/tech/Supabase.tsx
  - components/icons/tech/PostgreSQL.tsx
  - components/icons/tech/N8N.tsx
  - components/icons/tech/NodeJS.tsx
  - components/icons/tech/index.ts
  - components/sections/TechStack.tsx
  - components/sections/About.tsx
  - components/sections/Footer.tsx
autonomous: true
requirements: [TECH-01, ABOUT-01, LAYOUT-03]

must_haves:
  truths:
    - "Tech stack section displays technology logos as a subtle horizontal row"
    - "Tech stack logos are muted/monochrome with grayscale filter"
    - "About section displays bio text including London base, experience, stack, and music detail"
    - "About section includes a photo placeholder alongside bio text"
    - "Footer displays copyright '(c) 2026 Mira Consultancy'"
    - "Footer displays 'London, UK'"
    - "Footer displays LinkedIn, GitHub, and Email social icon links"
  artifacts:
    - path: "components/icons/LinkedIn.tsx"
      provides: "LinkedIn SVG icon component"
      min_lines: 5
    - path: "components/icons/GitHub.tsx"
      provides: "GitHub SVG icon component"
      min_lines: 5
    - path: "components/icons/Email.tsx"
      provides: "Email SVG icon component"
      min_lines: 5
    - path: "components/icons/tech/index.ts"
      provides: "Barrel export for tech stack icons"
    - path: "components/sections/TechStack.tsx"
      provides: "Tech stack section with logo icons"
      min_lines: 20
    - path: "components/sections/About.tsx"
      provides: "About section with photo + bio layout"
      min_lines: 20
    - path: "components/sections/Footer.tsx"
      provides: "Footer with copyright, location, and social links"
      min_lines: 20
  key_links:
    - from: "components/sections/TechStack.tsx"
      to: "lib/data/tech-stack.ts"
      via: "import { techStack } from '@/lib/data/tech-stack'"
      pattern: "techStack.*filter"
    - from: "components/sections/TechStack.tsx"
      to: "components/icons/tech/index.ts"
      via: "import from '@/components/icons/tech'"
      pattern: "iconMap"
    - from: "components/sections/Footer.tsx"
      to: "components/icons/index.ts"
      via: "import from '@/components/icons'"
      pattern: "LinkedInIcon|GitHubIcon|EmailIcon"
---

<objective>
Create icon components (social + tech logos) and the remaining content sections: TechStack, About, and Footer. These sections require SVG icon assets that don't exist yet.

Purpose: Complete the visual sections that require icon assets — tech stack with muted logo row, about with photo + bio, and footer with social icon links. These are the sections that go beyond pure text.

Output: Icon component library + 3 section components, ready for page assembly in Plan 03-03.
</objective>

<execution_context>
@/Users/zachsaid/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zachsaid/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-content-sections/03-RESEARCH.md
@.planning/phases/02-ui-primitives-animation/02-01-SUMMARY.md
@components/ui/AnimatedSection.tsx
@components/ui/index.ts
@lib/data/tech-stack.ts
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create social and tech stack SVG icon components</name>
  <files>
    components/icons/LinkedIn.tsx
    components/icons/GitHub.tsx
    components/icons/Email.tsx
    components/icons/index.ts
    components/icons/tech/TypeScript.tsx
    components/icons/tech/React.tsx
    components/icons/tech/Supabase.tsx
    components/icons/tech/PostgreSQL.tsx
    components/icons/tech/N8N.tsx
    components/icons/tech/NodeJS.tsx
    components/icons/tech/index.ts
  </files>
  <action>
Create inline SVG React components for all icons needed by TechStack and Footer sections.

**Social icons** (for Footer) in `components/icons/`:

Each social icon follows this pattern:
```tsx
export function LinkedInIcon({ className }: { className?: string }) {
  return (
    <svg className={className} viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
      <path d="..." />
    </svg>
  )
}
```

Create:
- `LinkedIn.tsx` — LinkedIn brand icon (standard 24x24 viewBox, `fill="currentColor"`)
- `GitHub.tsx` — GitHub octocat icon (standard 24x24 viewBox, `fill="currentColor"`)
- `Email.tsx` — Mail/envelope icon (standard 24x24 viewBox, `fill="currentColor"`) — use a simple envelope path, not a brand icon

Use standard SVG paths from Simple Icons (linkedin, github) or Heroicons (envelope for email). All icons must use `fill="currentColor"` so they respond to Tailwind text color utilities.

**Social icons barrel** — `components/icons/index.ts`:
```ts
export { LinkedInIcon } from './LinkedIn'
export { GitHubIcon } from './GitHub'
export { EmailIcon } from './Email'
```

**Tech stack icons** (for TechStack section) in `components/icons/tech/`:

The requirement specifies these technologies: TypeScript, React, Supabase, PostgreSQL, n8n, Node.js. Create icon components ONLY for these 6 technologies. The requirement says to display them as "a subtle row" — these are the ones that matter.

Each tech icon follows the same pattern as social icons: `({ className }: { className?: string })` accepting className prop, using `fill="currentColor"` for Tailwind compatibility. Use standard SVG paths from Simple Icons or tech-stack-icons.com.

Create:
- `TypeScript.tsx` — TypeScript logo SVG
- `React.tsx` — React atom logo SVG
- `Supabase.tsx` — Supabase logo SVG
- `PostgreSQL.tsx` — PostgreSQL elephant logo SVG
- `N8N.tsx` — n8n logo SVG
- `NodeJS.tsx` — Node.js logo SVG

**Tech icons barrel** — `components/icons/tech/index.ts`:
```ts
export { TypeScriptIcon } from './TypeScript'
export { ReactIcon } from './React'
export { SupabaseIcon } from './Supabase'
export { PostgreSQLIcon } from './PostgreSQL'
export { N8NIcon } from './N8N'
export { NodeJSIcon } from './NodeJS'
```

IMPORTANT: All SVG paths must use `fill="currentColor"` (not hardcoded colors) so the `grayscale` and `opacity` Tailwind utilities work correctly on the TechStack section. Remove any hardcoded fill/stroke colors from the SVG paths.
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Verify all icon files exist. Verify barrel exports work: `import { LinkedInIcon, GitHubIcon, EmailIcon } from '@/components/icons'` and `import { TypeScriptIcon, ReactIcon, SupabaseIcon, PostgreSQLIcon, N8NIcon, NodeJSIcon } from '@/components/icons/tech'`.
  </verify>
  <done>
9 SVG icon components exist (3 social + 6 tech) as React components with `fill="currentColor"` and className prop. Two barrel exports functional at components/icons/index.ts and components/icons/tech/index.ts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TechStack, About, and Footer section components</name>
  <files>
    components/sections/TechStack.tsx
    components/sections/About.tsx
    components/sections/Footer.tsx
  </files>
  <action>
Create the three remaining section components that require icon/image assets.

**TechStack.tsx** (TECH-01):
- Import `techStack` from `@/lib/data/tech-stack`
- Import `AnimatedSection` from `@/components/ui`
- Import all tech icons from `@/components/icons/tech`
- Create an `iconMap` object mapping tech IDs from data to icon components. The requirement specifies 6 technologies to display: TypeScript, React, Supabase, PostgreSQL, n8n, Node.js. Map these IDs from the tech-stack data file to the icon components:
  ```ts
  const iconMap: Record<string, React.ComponentType<{ className?: string }>> = {
    typescript: TypeScriptIcon,
    react: ReactIcon,
    supabase: SupabaseIcon,
    postgresql: PostgreSQLIcon,
    n8n: N8NIcon,
    nodejs: NodeJSIcon,
  }
  ```
- Render as a single horizontal row of icons (NOT grouped by category — the user wants a simple "subtle row"):
  - `<AnimatedSection as="section" className="section-padding">`
  - `<div className="prose-width">`
  - `<p className="section-label mb-8">Tech Stack</p>` — subtle muted label, no h2 heading (tech stack is understated)
  - `<div className="flex flex-wrap items-center gap-8">` — horizontal row with wrapping
  - Filter techStack to only items that have an icon in iconMap, then map each to:
    ```tsx
    <div key={tech.id} className="opacity-40 hover:opacity-100 transition-opacity duration-200" title={tech.name}>
      <Icon className="w-8 h-8" />
    </div>
    ```
  - Use `opacity-40` (not grayscale) for muted effect since icons use `currentColor` — they're already monochrome matching text color. Hover reveals at full opacity.
  - `w-8 h-8` (32px) icon size — subtle and scannable

**About.tsx** (ABOUT-01):
- Import `AnimatedSection` from `@/components/ui`
- Import `Image` from `next/image`
- Wrap in `<AnimatedSection as="section" className="section-padding">`
- Inner `<div className="prose-width">`
- `<p className="section-label mb-4">About</p>` — subtle label
- `<h2 className="mb-12">Who I Am</h2>`
- Grid layout: `<div className="grid md:grid-cols-[200px_1fr] gap-8 items-start">`
  - Photo column: `<div className="w-48 h-48 md:w-full md:h-auto rounded-lg overflow-hidden bg-text-tertiary/10">` — rounded corners, subtle placeholder background
    - Use Next.js `<Image>` with `src="/images/profile.jpg"`, `alt="Zach Said"`, `width={200}`, `height={200}`, `className="object-cover w-full h-full"`, `unoptimized` (required for static export)
  - Bio column: `<div className="space-y-4">`
    - Paragraph 1: "I'm a full-stack developer and technical consultant based in London. Since 2018, I've been building software for startups, agencies, and scale-ups across Europe and North America."
    - Paragraph 2: "My stack centres around TypeScript, React, Next.js, and Supabase, with a focus on PostgreSQL for data architecture and n8n for workflow automation."
    - Paragraph 3: "Outside of work, I produce electronic music and play live sets across London."
    - All paragraphs use `text-text-secondary` color
- Responsive: stacks vertically on mobile (photo then text), side-by-side on md+ screens
- Photo left, text right per research recommendation (Z-pattern reading flow)

**Footer.tsx** (LAYOUT-03):
- Import `LinkedInIcon, GitHubIcon, EmailIcon` from `@/components/icons`
- Render as `<footer>` element (semantic HTML)
- `<footer className="border-t border-text-tertiary/10 py-8">`
- Inner `<div className="prose-width">`
- Row 1: `<div className="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-2 mb-6 text-sm text-text-tertiary">`
  - `<p>&copy; 2026 Mira Consultancy</p>` — copyright text
  - `<p>London, UK</p>` — location
- Row 2: `<div className="flex items-center gap-6">`
  - LinkedIn: `<a href="https://linkedin.com/in/placeholder" target="_blank" rel="noopener noreferrer" className="text-text-secondary hover:text-text-primary transition-colors" aria-label="LinkedIn profile"><LinkedInIcon className="w-5 h-5" /></a>`
  - GitHub: same pattern with `href="https://github.com/placeholder"` and `aria-label="GitHub profile"`
  - Email: `<a href="mailto:hello@mira.co" className="text-text-secondary hover:text-text-primary transition-colors" aria-label="Email contact"><EmailIcon className="w-5 h-5" /></a>` — no target="_blank" for mailto
- Icon links: `w-5 h-5` (20px), `text-text-secondary` default, `hover:text-text-primary` on hover
- All external links have `aria-label` for accessibility (icon-only links)
- Footer does NOT use AnimatedSection — it's a static landmark element

After creating these 3 files, update `components/sections/index.ts` to add the new exports:
```ts
export { TechStack } from './TechStack'
export { About } from './About'
export { Footer } from './Footer'
```

Append these exports to the existing barrel file (do not overwrite — Plan 03-01 creates the initial exports).
  </action>
  <verify>
Run `npx tsc --noEmit` — no TypeScript errors. Verify TechStack renders 6 tech icons from data. Verify About has Image component with `unoptimized` prop. Verify Footer has `aria-label` on all social links. Verify sections barrel exports all 8 section components.
  </verify>
  <done>
TechStack section displays 6 technology logos in a subtle horizontal row with muted opacity. About section has grid layout with photo placeholder + 3-paragraph bio. Footer has two-row layout with copyright/location and 3 accessible social icon links. Sections barrel export updated with TechStack, About, Footer.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. 9 icon components exist (3 social, 6 tech) with `fill="currentColor"`
3. TechStack section renders 6 tech logos in horizontal row with opacity-40 muted effect
4. About section has grid layout (stacked mobile, side-by-side desktop) with photo + bio
5. About uses `<Image unoptimized>` for static export compatibility
6. Footer shows "(c) 2026 Mira Consultancy" and "London, UK"
7. Footer has 3 social links (LinkedIn, GitHub, Email) with aria-labels
8. Footer uses semantic `<footer>` element with border-top divider
9. sections/index.ts exports all 8 components (Hero, Intro, Services, Process, Contact, TechStack, About, Footer)
</verification>

<success_criteria>
- All icon components render correctly with currentColor fill
- TechStack displays a subtle row of tech logos (not text, not badges — actual logos)
- About section includes photo and bio with London, experience, stack, music details
- Footer has two-row layout with copyright/location and icon-only social links
- All social icon links have aria-label for accessibility
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-content-sections/03-02-SUMMARY.md`
</output>
